spring:
  rabbitmq:
    host: 127.0.0.1
    port: 5672
    username: guest
    password: guest
management:
  endpoints:
    web:
      exposure:
        include: refresh,busrefresh,busshutdown
text-to-print: "Текст ПОСЛЕ изменений" # проперти в микросервисе с RefreshScope